{% extends "base.njk" %}

{% block content %}
    <h1>{{ title }}</h1>
    <section>
        <h3>{{ section_1 }}</h3>
        <p>{{ caption_1 }}</p>
    </section>
    <section>
        <h3>{{ section_2 }}</h3>
        <p>{{ caption_2 }}</p>
    </section>
    {{ content | safe }}
{% endblock %}