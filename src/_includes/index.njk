{% extends "base.njk" %}

{% block content %}
    <section>
        <h1>{{ website_title }}</h1>
        <div class="captions">
            <p class="left_caption">{{ left_caption }}</p>
            <div class="button">
                <button type="button">{{ button }}</button>
            </div>
            <p class="right_caption">{{ right_caption }}</p>
        </div>
    </section>
    <hr>
{% endblock %}